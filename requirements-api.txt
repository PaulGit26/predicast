# Runtime-only dependencies for the FastAPI service.
# Heavy ML orchestration (mlflow, prefect, ray, dask, feast) belongs in requirements.txt
# and is only needed for training — not for serving predictions.

# Core
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# API
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
PyJWT>=2.8.0
cryptography>=41.0.0

# Data / ML inference
numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=2.0.3
joblib>=1.3.0
scipy>=1.11.0

# Database & cache
sqlalchemy>=2.0.23
redis>=5.0.0

# Observability
python-json-logger>=2.0.7
structlog>=23.2.0
sentry-sdk>=1.38.0
